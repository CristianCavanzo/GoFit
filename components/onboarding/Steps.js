import React from 'react';
import styled from 'styled-components';

const SectionSteps = styled.section`
    display: flex;
    flex-direction: column;
    width: 100vw;
    height: 100vh;
`;

const Steps = () => {
    return <SectionSteps></SectionSteps>;
};

export { Steps };
